import { DownloadSvgDirective } from './download-svg.directive';

describe('DownloadSvgDirective', () => {
  it('should create an instance', () => {
    const directive = new DownloadSvgDirective();
    expect(directive).toBeTruthy();
  });
});
